import React from 'react';
import BaseView from "../bases/BaseView";

const PageAuthenticate = () => {
    return <BaseView style={{backgroundColor: '#000'}}/>;
};

export default PageAuthenticate;

